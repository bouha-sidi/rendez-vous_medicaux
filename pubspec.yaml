name: mofi_app
description: "A new Flutter project."
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.10.1

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # ✅ API + Upload photo + Token
  http: ^1.2.2
  image_picker: ^1.1.2
  http_parser: ^4.0.2
  mime: ^1.0.5
  intl: ^0.19.0
  shared_preferences: ^2.5.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
